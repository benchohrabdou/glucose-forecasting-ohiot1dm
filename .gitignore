# Raw data is under a Data Use Agreement: never commit it or anything derived from it.
# Anchored: an unanchored "data/" would also hide the source package src/data/.
/data/
src/data/raw/
src/data/processed/
*.xml

# Model artifacts
checkpoints/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Shows a real 8 h CGM trace from a test file (closer to raw data than aggregate results):
# held back until the data owner confirms it may be published.
results/figures/predicted_vs_actual*.png
